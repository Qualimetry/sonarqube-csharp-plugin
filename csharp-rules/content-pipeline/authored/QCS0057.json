{
  "id": "QCS0057",
  "title": "Abstract type constructors should be protected or private",
  "tags": [
    "design",
    "clarity"
  ],
  "messageFormat": "Declare this constructor of an abstract type as 'protected' or 'private' rather than widely accessible.",
  "summary": [
    "<p>An abstract type can only be instantiated through a derived class, so a constructor that is <code>public</code> or <code>internal</code> advertises an entry point that no caller can ever use. It misleads readers about how the type is meant to be created.</p>",
    "<p>Constructors of abstract types exist solely for derived classes to chain into. Declare them <code>protected</code> (or <code>private</code> when only the type's own code chains to them) to state that contract precisely.</p>"
  ],
  "noncompliant": "public abstract class Repository\n{\n    internal Repository(string name) // Noncompliant\n    {\n        Name = name;\n    }\n\n    public string Name { get; }\n}",
  "compliant": "public abstract class Repository\n{\n    protected Repository(string name)\n    {\n        Name = name;\n    }\n\n    public string Name { get; }\n}",
  "seeAlso": [
    {
      "text": "Abstract classes (C# language reference)",
      "href": "https://learn.microsoft.com/dotnet/csharp/language-reference/keywords/abstract"
    }
  ],
  "ruleKey": "qa_quality_abstract_type_constructor_accessibility"
}
