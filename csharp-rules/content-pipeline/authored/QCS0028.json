{
  "id": "QCS0028",
  "title": "The singleton pattern should be avoided",
  "tags": [
    "design",
    "testability"
  ],
  "messageFormat": "Avoid the hand-rolled singleton; expose this type through dependency injection with an explicit lifetime.",
  "summary": [
    "<p>A type that hides a private constructor behind a static <code>Instance</code> accessor builds global mutable state into the type itself. Every consumer reaches for it implicitly, which makes dependencies invisible, couples code to a single concrete instance, and makes the type almost impossible to substitute in a test. Register the type with a container and inject it, so its lifetime is explicit and a fake can take its place where needed.</p>"
  ],
  "noncompliant": "public sealed class ConfigStore // Noncompliant\n{\n    private static readonly ConfigStore _instance = new ConfigStore();\n\n    private ConfigStore()\n    {\n    }\n\n    public static ConfigStore Instance => _instance;\n}",
  "compliant": "public sealed class ConfigStore\n{\n    public ConfigStore()\n    {\n    }\n}",
  "ruleKey": "qa_quality_singleton_pattern"
}
