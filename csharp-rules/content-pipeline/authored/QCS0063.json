{
  "id": "QCS0063",
  "title": "Readonly fields of mutable reference types give a false sense of immutability",
  "tags": [
    "suspicious",
    "design"
  ],
  "messageFormat": "This readonly field holds a mutable type; readonly stops reassignment but not mutation of the contents.",
  "summary": [
    "<p>The <code>readonly</code> modifier freezes only the reference, not the object it points to. A readonly field typed as an array or a mutable collection still lets any holder add, remove, or overwrite elements, so the field reads as immutable while its contents are wide open. Expose a read-only view such as <code>IReadOnlyList&lt;T&gt;</code>, or use an immutable collection type, when the intent is that the contents cannot change.</p>"
  ],
  "noncompliant": "using System.Collections.Generic;\n\npublic sealed class Registry\n{\n    private readonly List<string> _names = new List<string>(); // Noncompliant\n}",
  "compliant": "using System.Collections.Generic;\n\npublic sealed class Registry\n{\n    private readonly IReadOnlyList<string> _names = new List<string>();\n}",
  "ruleKey": "qa_quality_readonly_mutable_reference_field"
}
