{
  "id": "QCS0110",
  "title": "Methods prefixed with Try should return bool",
  "tags": [
    "naming",
    "clarity"
  ],
  "messageFormat": "Make this Try-prefixed method return bool to follow the Try-pattern convention.",
  "summary": [
    "<p>By convention a method whose name starts with <code>Try</code> signals success or failure through a <code>bool</code> result and returns any produced value through an <code>out</code> parameter. A <code>Try</code> method that returns something other than <code>bool</code> contradicts that expectation and confuses callers.</p>"
  ],
  "noncompliant": "public int TryParseCount(string text) // Noncompliant\n{\n    return int.Parse(text);\n}",
  "compliant": "public bool TryParseCount(string text, out int count)\n{\n    return int.TryParse(text, out count);\n}",
  "seeAlso": [
    {
      "text": "Exceptions and the Try-Parse pattern",
      "href": "https://learn.microsoft.com/dotnet/standard/design-guidelines/exceptions-and-performance"
    }
  ],
  "ruleKey": "qa_quality_try_method_return_type"
}
