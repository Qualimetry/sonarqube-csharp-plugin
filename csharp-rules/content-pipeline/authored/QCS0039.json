{
  "id": "QCS0039",
  "title": "Extension methods should be called through member access",
  "tags": [
    "convention",
    "clarity",
    "linq"
  ],
  "messageFormat": "Invoke this extension method on its receiver instead of as a static call.",
  "summary": [
    "<p>An extension method exists so that it reads as if it belonged to the receiver type. Calling it through the declaring class as a plain static method discards that benefit and makes a fluent chain harder to follow.</p>",
    "<p>Rewriting the call as member access on the first argument keeps related operations on one subject reading left to right, which matches how the rest of the API is used.</p>"
  ],
  "noncompliant": "var trimmed = StringExtensions.Shorten(name, 10); // Noncompliant",
  "compliant": "var trimmed = name.Shorten(10);",
  "seeAlso": [
    {
      "text": "Extension methods (C# programming guide)",
      "href": "https://learn.microsoft.com/dotnet/csharp/programming-guide/classes-and-structs/extension-methods"
    }
  ],
  "ruleKey": "qa_style_extension_method_invocation_style"
}
